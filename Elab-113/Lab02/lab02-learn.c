#include <stdio.h>
#include <stdlib.h>

int main()
{
    // char str[5];
    // //gets(str); //not use because keep more than make variable
    // fgets(str, 5, stdin); //if want keep 10 string must make variable +2 (10 + 2)
    // printf("[%s]\n", str);

    // //int i = atoi(str);
    // double f = atof(str);
    // long l = atol(str);
    // printf("%lf\n", f);

    // char c = getchar(); //getchar keep \n
    // printf("%s %c\n", str, c);
    
    //bool isExist = true; //in c not datatype boolean.
    // int isExist = 1;
    // int isDigit[] = ""; //true

    // // false -> 0, 0.0, '\0'
    // char a = '\0';


    // ------------If else---------------
    // relarional operator
    // > < >= <= == != -> 0,

    // logical operator
    // && || !

    // char str[20];
    // fgets(str, 20, stdin);

    // double score = atof(str);

    // if (score >= 90) {
    //     printf("A");
    // } else if (score >= 80){
    //     printf("B");

    // } else {
    //     printf("F");
    // }

    //-----------loop----------
    // int i = 0;
    // while (i < 20)
    // {
    //     printf("%d\n", i);i++;
    // }

    // for (int i = 0; i < 20; i++)
    // {
    //     printf("%d\n", i);
    // }

    // while (1)
    // {
    //     break;
    //     continue;
    // }
}